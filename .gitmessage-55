Round 55: SDK W3C trace context propagation parity

- Go SDK: Exporter now injects traceparent via Propagator; added
  WithTraceID, WithParentSpanID, WithSampled, WithTracestate,
  Sampled public helpers; exporter_traceparent_test verifies the
  header shape
- Node SDK: setCurrent(trace,span) caches the active context;
  _post() emits a traceparent header on the next ingest; new
  traceparent_test.js exercises end-to-end with a local HTTP server
- Python SDK: set_current(trace,span) public method; _post() adds
  the W3C traceparent header when a current trace is set

Java SDK: already had W3C-friendly state; no changes needed.
